# US0000009 — Estado da Revisão Pendente

**Data:** 2026-03-30
**Branch:** `feat/86aghxvba-redesign-layout-ui`
**Último commit:** `9fb3aa3`
**ClickUp:** [US0000009](https://app.clickup.com/t/86aghxvba)

## Problema

Após o rebase da branch `feat/86aghcd2w` (US0000006) para `main` e criação da nova branch `feat/86aghxvba`, houve conflitos de merge. A resolução dos conflitos causou perda de funcionalidades que existiam na implementação original.

## Bugs Identificados

### 1. Menu lateral (Sidebar/OrgPanel) não aparece
- O `Sidebar.tsx` foi reescrito com `if (!orgPanelOpen) return null` e `hidden md:flex`
- O `IconSidebar.tsx` controla `orgPanelOpen` via botão de Building2
- **Possível causa:** O `orgPanelOpen` inicia como `true` mas o `hidden md:flex` pode estar escondendo no viewport atual, OU o IconSidebar não está renderizando (verificar se `TooltipProvider` está envolvendo corretamente)

### 2. Botão de dark/light mode sumiu
- O `ThemeToggle` foi movido para dentro do `IconSidebar` (não está mais no Header)
- O Header antigo (`components/layout/Header.tsx`) não é mais importado no `page.tsx`
- **Possível causa:** Se o `IconSidebar` não está renderizando, o ThemeToggle também não aparece. Verificar se o `IconSidebar` tem `hidden md:flex` — no mobile ele não aparece, e no desktop precisa verificar se está dentro do card flutuante

### 3. Possíveis outros itens perdidos no rebase
- `favicon.svg` — verificar se foi incluído
- `.gitignore` updates — verificar
- `EmptyChat.tsx` vs `EmptyState.tsx` — na main existe `EmptyChat.tsx`, a branch nova usa `EmptyState.tsx`

## Arquivos Críticos para Revisão

| Arquivo | Status | O que verificar |
|---------|--------|-----------------|
| `app/chat/page.tsx` | Reescrito | `IconSidebar` importado e renderizado? `TooltipProvider` envolvendo? |
| `components/layout/IconSidebar.tsx` | Novo | Está sendo renderizado? Classes `hidden md:flex` corretas? |
| `components/layout/Sidebar.tsx` | Reescrito | `orgPanelOpen` funciona? `hidden md:flex` conflita com algo? |
| `components/layout/ThemeToggle.tsx` | Reescrito | Depende do IconSidebar renderizar |
| `components/layout/Header.tsx` | Não importado mais | Verificar se mobile header no page.tsx substitui adequadamente |
| `stores/chatStore.ts` | Corrigido (commit 9fb3aa3) | Loading states + removeConversation adicionados |
| `stores/uiStore.ts` | Atualizado | `orgPanelOpen` + `toggleOrgPanel` + removido `mobileView` da versão main |
| `app/api/conversations/[id]/route.ts` | Corrigido (commit 9fb3aa3) | PATCH handler adicionado |
| `public/favicon.svg` | Verificar | Pode não ter sido incluído no rebase |

## Como Testar

1. `npm run dev`
2. Verificar desktop (1400x900):
   - IconSidebar escura à esquerda com "A", ícones, theme toggle
   - OrgPanel com "Organizações" header
   - ConversationList com tabs pill
   - ChatPanel com bg-chat-bg
3. Verificar mobile (375x812):
   - Sem IconSidebar
   - Mobile header com hamburger + brand
   - Alternância conversas/chat
4. Testar dark/light mode toggle
5. Testar seleção org → phone → conversa → mensagens
6. Testar arquivar conversa

## Screenshots de Referência (antes do rebase)

Os screenshots `test-16` a `test-18` em `docs/references/2026-03-30-melhorar-ux-loading-responsividade/` mostram como o layout DEVERIA estar:
- `test-16-redesign-desktop-empty.png` — Layout correto com IconSidebar + OrgPanel
- `test-17-redesign-chat-light.png` — Chat em light mode com avatares nos inbound
- `test-18-redesign-chat-dark.png` — Chat em dark mode com bolhas teal

## Contexto da Conversa

Esta US passou por 3 fases nesta sessão:
1. **US0000006** (UX improvements) — loading states, skeletons, empty states, responsividade, archive — branch `feat/86aghcd2w` commitada e pushada
2. **US0000009 fase 1** (tokens/animações) — design tokens, easing curves, fonts, button polish — aplicado sobre a branch anterior
3. **US0000009 fase 2** (redesign completo) — card flutuante, IconSidebar, paleta teal, MessageBubble com avatar — criou nova branch `feat/86aghxvba` a partir de main, causando conflitos

O rebase perdeu itens da US0000006 (skeletons, loading states, PATCH handler, removeConversation) que foram re-adicionados nos commits de fix, mas a **verificação visual não foi refeita** após os fixes.
